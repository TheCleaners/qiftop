#!/bin/sh
# postrm for qiftop-agent
set -e

case "$1" in
    remove|purge|upgrade|deconfigure)
        if command -v systemctl >/dev/null 2>&1; then
            systemctl stop qiftop-agent.service 2>/dev/null || true
            systemctl daemon-reload || true
        fi
        ;;
esac

exit 0
